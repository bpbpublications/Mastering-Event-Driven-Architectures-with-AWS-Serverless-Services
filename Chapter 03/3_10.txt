#python
import boto3
from aws_xray_sdk.core import xray_recorder
from aws_xray_sdk.core import patch_all

patch_all()

def lambda_handler(event, context):
	# Tracing the event
	xray_recorder.begin_subsegment('ProcessingOrder')
    
	try:
    	# Process the event
    	process_order(event)
	finally:
    	xray_recorder.end_subsegment()

