//TypeScript
import * as lambda from 'aws-cdk-lib/aws-lambda';
 const fn = new lambda.Function(this, 'FastHandler', {
 runtime: lambda.Runtime.NODEJS_18_X,
 handler: 'index.handler',
 code: lambda.Code.fromAsset('lambda'),
 memorySize: 1024,
 });
 // Allocate 10 provisioned concurrency (on a published version or alias)
 fn.currentVersion.addAlias('Live', { provisionedConcurrentExecutions: 10 });

